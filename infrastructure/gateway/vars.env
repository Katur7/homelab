# Gateway stack — non-sensitive configuration
# Git-tracked. Secrets go in .env (git-ignored).
#
# TZ, PUID, PGID, DOMAIN, INTERNAL_DOMAIN, TUNNEL_NETWORK
# are inherited from ../../global.env — do not duplicate here.
